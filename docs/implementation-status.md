# Implementation Status

Last updated: 2026-05-27

## Current UI Shell

| Area                 | Status | Evidence                                                                                                                                                                                        |
| -------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Intro/service split  | Wired  | `/` renders the pre-service introduction screen and its primary CTA enters `/home`.                                                                                                             |
| Mobile service frame | Wired  | Service routes render inside a 480 x 850 mobile frame on desktop; intro/auth keep mobile chrome.                                                                                                |
| Service home         | Wired  | `/home` renders greeting, environment status, AI curation CTA, quick navigation cards including theme exploration, a draggable weekend healing carousel, and store-backed community hot issues. |

## Performance Notes

| Area                      | Status | Evidence                                                                                                                                                                                          |
| ------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Query reuse               | Wired  | Router-level React Query defaults keep data fresh for 5 minutes, retain cache for 30 minutes, disable focus refetching, and cap retries at 1.                                                     |
| Home environment loading  | Wired  | Weather, air-quality, and reverse-geocoding requests run in parallel instead of sequentially.                                                                                                     |
| Deferred auth bundle load | Wired  | The app chrome defers Supabase auth initialization until browser idle; auth and mypage routes still initialize immediately.                                                                       |
| Map render responsiveness | Wired  | Map text filtering uses deferred input, map callbacks are stable, and mobile list cards use `content-visibility: auto`.                                                                           |
| Facility detail reuse     | Wired  | Facility detail checks the Supabase-pinned gallery first; when found, it renders from cached image metadata without TourAPI search/detail calls and keeps React Query detail data for 30 minutes. |

## Phase 0 Verification

| Area                | Status          | Evidence                                                                                                     |
| ------------------- | --------------- | ------------------------------------------------------------------------------------------------------------ |
| Test suite          | Pass            | `npm test`                                                                                                   |
| Type check          | Pass            | `npx tsc --noEmit`                                                                                           |
| Production build    | Pass on Node 22 | `npm run build` uses `scripts/build-with-supported-node.cjs` to avoid the Node 24 Windows Vite/Rollup crash. |
| Vercel SSR output   | Fixed           | `vite.config.ts` includes Nitro's Vercel preset so `npm run build` emits `.vercel/output/config.json` and routes all non-static requests to `__server`. |
| Lint                | Pass target     | Source files were normalized with Prettier.                                                                  |
| Supabase client URL | Fixed           | `VITE_SUPABASE_URL` now points to the project root URL, not `/rest/v1/`.                                     |

## Phase 1 Verification

| Area                       | Status | Evidence                                                                                   |
| -------------------------- | ------ | ------------------------------------------------------------------------------------------ |
| Supabase response mapping  | Pass   | `src/lib/api-client-persistence.test.ts` covers health, recommendation, visit, saved rows. |
| Recommendation persistence | Wired  | Recommendation generation saves first and returns the persisted recommendation id.         |
| Result detail recovery     | Wired  | `/result/$id` loads persisted recommendation data when in-memory state is empty.           |
| Visit record history       | Wired  | `/records` fetches persisted visit records and derives summary metrics.                    |
| Saved course action        | Wired  | Result detail save button invokes `saved-courses`.                                         |
| Survey value normalization | Pass   | `src/stores/surveyStore.test.ts` verifies numeric `maxTravelTime`.                         |

## Phase 2 Verification

| Area                          | Status | Evidence                                                                                                                          |
| ----------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------- |
| Public facility candidate use | Wired  | Recommendation API combines static healing forests with coordinate-backed recreation forests when a public-data key is available. |
| Profile-based scoring         | Pass   | `src/lib/recommendation-scoring.test.ts` covers accessibility, activities, fitness, and choice.                                   |
| Program enrichment            | Pass   | Matched forest education programs are merged into coordinate-backed facility candidates for scoring and map program chips.        |
| Mock base removal             | Pass   | API route tests verify recommendation candidates come from public facility ids, not `f-001`.                                      |
| Gemini role                   | Wired  | Gemini now receives the scored public-data candidate as base context and only refines content.                                    |

## Phase 3 Verification

| Area                           | Status  | Evidence                                                                                                                           |
| ------------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| Environment request snapshot   | Pass    | `apiClient.generateRecommendation` includes weather, air quality, UV index, and UV level.                                          |
| Recommendation environment use | Pass    | Recommendation API route test verifies poor weather/air/UV lowers environmental suitability.                                       |
| Environment store population   | Wired   | Weather, air quality, and UV hooks write successful API results into `appStore.environment`.                                       |
| Home environment preload       | Retired | Home route is now a Template-style landing page; environment data remains part of recommendation generation, not the landing hero. |
| Program search route           | Removed | `/programs` no longer exposes forest education search; direct access redirects to `/curation`.                                     |
| Education timeout guard        | Pass    | `src/lib/api-client.test.ts` covers client timeout handling for stalled education proxy calls.                                     |

## Phase 4 Verification

| Area                     | Status  | Evidence                                                                                                                                                                     |
| ------------------------ | ------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Home landing replacement | Wired   | `/` renders the Template-style glassmorphism landing page directly with `/홈화면 배경영상 16초.webp` as the animated background.                                             |
| Home environment summary | Retired | `buildHomeEnvironmentSummary` remains tested as utility code, but the current home landing no longer renders the environment card.                                           |
| Location request CTA     | Retired | Location permission is no longer requested on the landing page; recommendation and map flows keep their own data needs.                                                      |
| Static weather removal   | Pass    | Home route test verifies the landing page no longer contains the previous hard-coded weather card wiring.                                                                    |
| Dynamic badges           | Retired | Dynamic environment badges are not part of the Template-style landing page.                                                                                                  |
| Plant image API route    | Retired | The cleaned `산림청_숲에 사는 식물 정보_20240826_정제.csv` dataset has no image fields; `/api/forest-plant-images` remains compatibility-only and returns `NO_IMAGE_FIELDS`. |
| Plant image client       | Retired | `apiClient.getPlantImages()` remains available for compatibility, but the current `/plants` UI does not call it.                                                             |
| Plant image UI lookup    | Retired | `/plants` displays that the cleaned CSV does not provide image fields instead of showing image file metadata.                                                                |
| Plant CSV timeout guard  | Pass    | Plant list lookup uses a 12s client timeout and disables query retries.                                                                                                      |
| SSR Supabase build guard | Pass    | Supabase browser client is lazy-loaded and `@supabase/node-fetch` is aliased to its browser shim for Cloudflare SSR builds.                                                  |

## Phase 5 Verification

| Area                        | Status | Evidence                                                                                                                                                       |
| --------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Visit effect analysis logic | Pass   | `src/lib/effect-analysis.test.ts` covers minimum record count, positive trend, and negative trend.                                                             |
| Visit record creation form  | Wired  | `/records` now submits facility, date, duration, activity, stress, sleep, mood, and memo fields.                                                               |
| Records page analysis panel | Wired  | `/records` renders a 3-record threshold and deterministic record-based effect analysis.                                                                        |
| Data curation builder       | Pass   | `src/lib/data-curation.test.ts` covers public-data based card ids, copy, fallback locations, and item count.                                                   |
| Static home curation        | Wired  | `/home` renders facility-derived public-data curation cards in a horizontal drag carousel.                                                                     |
| Home curation data binding  | Wired  | Facility-derived curation remains available in map filtering logic and feeds the `/home` weekend healing carousel.                                             |
| Curation navigation         | Wired  | Direct `/map?curation={id}` URLs apply map marker/list filtering and show the actual displayed facility count with recommendation criteria and API data basis. |

## Phase 6 Verification

| Area                     | Status | Evidence                                                                                                                               |
| ------------------------ | ------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| Saved course page wiring | Pass   | `src/routes/-mypage-saved-courses.test.ts` verifies hook usage, empty state, and result link.                                          |
| Saved course list UI     | Wired  | `/mypage` fetches saved courses and renders loading, login, error, empty, and list states.                                             |
| Saved course reuse       | Wired  | Saved courses with `recommendationId` link back to `/result/$id`.                                                                      |
| Plant guide logic        | Pass   | `src/lib/plant-guide.test.ts` covers current-month flowering priority, search, and helper fallback behavior.                           |
| Plant guide page         | Wired  | `/plants` sends the search query to the CSV-backed plant hook and no longer renders local example cards on lookup failure.             |
| Plant camera capture     | Wired  | `/plants` opens a `getUserMedia` camera preview for direct capture and sends the captured frame through the existing recognition flow. |
| Forest plant CSV route   | Fixed  | `/api/forest-plants` has a TanStack file route wired to `handleForestPlantsApiRequest`; it reads the cleaned CSV instead of ODCloud.   |
| Home plant guide entry   | Wired  | Template-style home landing links to `/plants`.                                                                                        |

## Phase 7 Verification

| Area                              | Status | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Traditional village forest parser | Pass   | `src/lib/traditional-village-forest.test.ts` covers request params and XML response mapping.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Naver geocoding parser            | Pass   | `src/lib/naver-geocoding.test.ts` covers request URL, key headers, and coordinate normalization.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| API proxy routes                  | Wired  | `/api/traditional-village-forests` and `/api/geocode` are TanStack server routes.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Map facility integration          | Wired  | `apiClient.getFacilities()` merges SHP-derived arboretum point data as map facilities. Traditional village forest source data is retained but not used in map startup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Map filter                        | Wired  | Map filter chips expose marker-backed categories: `전체`, `치유의숲`, `자연휴양림`, `수목원`. Visible map categories use distinct marker colors: forest green for healing forests, lake blue for recreation forests, and garden amber for arboretums.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Facility detail page              | Wired  | Map overlay `상세정보` links to `/facilities/$facilityId`; healing forest, recreation forest, and SHP-derived arboretum records expose category-specific detail sections and source request/output metadata.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Facility detail source disclosure | Wired  | Contact/homepage values are shown once in the primary info card, while public-data and shapefile output fields are grouped under a collapsed `데이터 출처 / 원본 정보` section.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Facility usage info layout        | Wired  | Usage guidance now uses full-width rows for long operating-hour and fee text, with compact secondary rows for rest day, parking, and inquiry details.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| Homepage AI analysis API          | Pinned | `GET/POST /api/facility-homepage-analysis` validates public homepage URLs and reuses Supabase-pinned analysis results by homepage URL before any Gemini call. New Gemini results are saved in `facility_homepage_analyses`; the detail page auto-loads pinned results, removes forced re-analysis, and renders only user-facing section cards while hiding source URL and analysis-time cards.                                                                                                                                                                                                                                                                                                                                                                                |
| Facility detail tab navigation    | Wired  | `소개`, `이용안내`, `사진`, and `교통정보` tabs scroll to document-relative section positions and expose tab/section ARIA links for reliable click behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Facility photo gallery            | Pass   | `src/components/forest/PhotoGallerySection.test.ts` verifies empty, duplicate, and failed photo URLs are removed before gallery layout. Facility detail image search uses facility-name-first queries, caps candidates at 15, then `/api/facility-image-curation` sends downloaded image pixels to Gemini as `inlineData` and keeps only accepted, scored candidates. Kids forest image lookup removes duplicate `유아숲체험` keywords, adds city/district scoped helper queries from the address, and lets `facility`/`experience` images backfill the detail hero. Successful Gemini results are pinned in Supabase `facility_image_curations` by facility ID; later visits reuse that pinned gallery before image search and skip TourAPI detail loading for faster entry. |

Detail tab note: bottom-of-page scroll state keeps the final `교통정보` tab selected when the last section cannot align to the sticky tab offset.
Detail tab fix: tab navigation now uses the app shell's `#main-content` only when that element is actually scrollable; otherwise it falls back to `window`, matching both mobile and fixed-frame layouts.
Detail tab active-state note: section activation allows a small pixel tolerance so smooth-scroll rounding does not leave the previous tab selected at the target section.
Transport map note: NAVER Maps SDK authorization/load failure now falls back to external NAVER/Kakao route links instead of throwing repeated map initialization errors.

## Phase 8 Verification

| Area                         | Status   | Evidence                                                                                                        |
| ---------------------------- | -------- | --------------------------------------------------------------------------------------------------------------- |
| Saved course update/delete   | Wired    | `apiClient.updateSavedCourse()`, `apiClient.deleteSavedCourse()`, `useSavedCourses()`, and `/mypage` are wired. |
| Duplicate save prevention    | Wired    | `saved-courses` Edge Function returns an existing row when the same user saves the same `recommendationId`.     |
| Supabase saved-course policy | Deployed | Migration `20260522073457_phase8_saved_courses_update_policy.sql` is pushed to the remote project.              |
| Edge Functions deployment    | Deployed | `health-profile`, `recommendations`, `visit-records`, and `saved-courses` are deployed as version 5.            |

## Phase 9 Verification

| Area                              | Status | Evidence                                                                                                                                            |
| --------------------------------- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------- |
| Location-aware recommendation     | Pass   | `src/lib/gemini-recommendation-api-route.test.ts` verifies user coordinates prefer the reachable public-data candidate.                             |
| Recommendation request location   | Pass   | `src/lib/api-client.test.ts` verifies `apiClient.generateRecommendation()` sends `location: { lat, lng }` to `/api/recommendation`.                 |
| Gemini failure fallback           | Pass   | `src/lib/gemini-recommendation-api-route.test.ts` verifies Gemini failure returns the scored public-data base recommendation with `cached: true`.   |
| Local mock fallback removal       | Pass   | `src/lib/api-client.test.ts` verifies failed recommendation proxy responses are no longer hidden behind local mock data.                            |
| Recommendation environment gating | Pass   | `src/routes/-recommend-flow.test.ts` verifies `/recommend` loads geolocation, weather, air quality, and UV data before requesting a recommendation. |

## Current Implementation Map

| Feature                      | Status          | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ---------------------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Health survey                | Partial         | Seven-step survey saves through Supabase Edge Function for signed-in users. Anonymous users now create a local health profile and skip the `health-profile` call, preventing expected 401 console noise.                                                                                                                                                                                                                                                                                                                                                                                                                            |
| AI recommendation            | Partial         | Candidate scoring uses healing forests plus coordinate-backed recreation forests. Matched forest education program data is no longer merged because its period/telephone fields can conflict with official facility information. When user coordinates are available, rough travel-time estimates are applied before `maxTravelTime` scoring. Gemini failure falls back to the scored public-data base recommendation, not local mock data.                                                                                                                                                                                         |
| Weather and air quality APIs | Partial         | API proxy modules exist and recommendation assessment consumes loaded environment snapshots. `/recommend` now waits for weather, air quality, and UV query success/error when coordinates are available. The Template-style home landing no longer requests location automatically.                                                                                                                                                                                                                                                                                                                                                 |
| Facility map                 | Partial         | Naver Maps and fallback map expose healing forests, coordinate-backed recreation forests, and SHP-derived arboretums as selectable filters. Marker detail actions now open `/facilities/$facilityId` with category-specific source fields. Traditional village forest markers are removed. Forest education enrichment is disabled to avoid surfacing stale or wrong period/telephone data. Naver SDK auth/constructor failure now falls back instead of crashing the route. Current user coordinates are requested automatically on map entry, re-center the Naver map, and sort the map facility list by distance when available. |
| Kids forest map              | Partial         | `/kids-map` loads ODCloud 유아숲체험원 records through `/api/kids-forests` and merges static WGS84 coordinate seeds. Coordinate matching now prefers exact address matches and disables facility-name fallback for duplicated names. Region filtering uses administrative address prefixes so place names such as `대전리` or `대전면` do not leak into 충청권.                                                                                                                                                                                                                                                                     |
| Program search               | Removed         | User-facing forest education search was removed because the live Forest Service endpoint returned timeout/502/503-style failures in local verification. `/programs` redirects to `/curation`.                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Public data integration      | Partial         | Facility corpus merges healing forest, recreation forest, and SHP-derived arboretum data. Forest education program data remains available only through the inactive proxy/parser path and is not merged into user-facing facility cards.                                                                                                                                                                                                                                                                                                                                                                                            |
| Result persistence           | Implemented     | Signed-in recommendations are persisted before navigation, and result detail can reload by persisted id. Anonymous recommendations skip Supabase persistence and remain in app state.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Visit records                | Partial         | DB, hook, records page fetch, summary metrics, creation form, and deterministic record-based analysis exist. Edit/delete UI and Gemini-based effect analysis are not implemented yet.                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| Saved courses                | Implemented     | Result page save button, duplicate-safe `saved-courses` Edge Function, Supabase fetch/update/delete hook, and `/mypage` list/edit/delete/reopen UI are wired.                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| Data curation                | Partial         | Map curation URLs support `registered-healing-forest`, `activity-recreation-forest`, `small-capacity-recreation-forest`, and `lodging-recreation-forest` ids; map search remains user-driven while the curation summary shows the actual displayed facility count, recommendation criteria, and API basis. The `/curation` theme list no longer shows fixed `BEST N` labels and instead exposes each theme's filtering criteria. The home screen exposes theme exploration through the quick navigation card and the same data-driven themes through a draggable weekend healing carousel.                                          |
| Plant guide                  | Partial         | Searchable `/plants` screen and `/api/forest-plants` use `api가이드파일/산림청_숲에 사는 식물 정보_20240826_정제.csv` instead of the ODCloud plant API. The CSV has no image fields, so image lookup is retired and the UI states that the CSV does not provide images. Direct capture uses a browser camera preview before submitting the captured frame to plant recognition, then enriches Gemini results from the CSV when matched.                                                                                                                                                                                             |
| Congestion                   | Not implemented | Forest big data floating-population API is not integrated.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Directions/geocoding         | Partial         | Naver Geocoding server proxy is wired for address-to-coordinate conversion. Directions 5 now uses the `maps.apigw.ntruss.com` host through `/api/naver-direction`, keeps the client secret server-side, and falls back to external route links when route data is unavailable. Facility detail driving mode now expands the map to a full-viewport navigation layout with black guidance text, a top instruction card, right-side controls, and a bottom route dashboard. Reverse geocoding is not implemented yet.                                                                                                                 |

## Next Phase Entry Criteria

Next implementation phase should start only after `npm test`, `npx tsc --noEmit`, `npm run lint`, and `npm run build` pass in the local workspace.
